# Healthrytix Web

Minimal Vite + React + TypeScript + Tailwind setup for the supplied Healthrytix page.

## Run

```bash
npm install
npm run dev
```

Then open the local URL printed by Vite (normally http://localhost:5173).
